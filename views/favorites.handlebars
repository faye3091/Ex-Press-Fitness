<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{body_part}}</p>
  <p>Level: {{level}}</p>
  <p>Repetitions: {{repetitions}}</p>
  <p>Sets: {{sets}}</p>
  <p>
    <iframe
      width="560"
      height="315"
      src="{{video}}"
      title="Watch the workout here!"
      frameborder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowfullscreen
    ></iframe>
  </p>
</div>